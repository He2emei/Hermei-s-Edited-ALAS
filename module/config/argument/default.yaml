# --------------------
# Define default values
# --------------------

# ==================== Alas ====================

# ==================== Farm ====================

Main:
  StopCondition:
    MapAchievement: non_stop
    StageIncrease: false
GemsFarming:
  EquipmentCode:
    Config: |-
      DD: null
      bogue: null
      hermes: null
      langley: null
      ranger: null

# ==================== Event ====================

Event:
  StopCondition:
    MapAchievement: map_3_stars
    StageIncrease: true
Event2:
  StopCondition:
    MapAchievement: non_stop
    StageIncrease: false
EventA:
  EventDaily:
    StageFilter: |-
      A1 > A2 > A3
EventB:
  EventDaily:
    StageFilter: |-
      B1 > B2 > B3
EventC:
  EventDaily:
    StageFilter: |-
      C1 > C2 > C3
EventD:
  EventDaily:
    StageFilter: |-
      D1 > D2 > D3

# ==================== Reward ====================

Commission:
  Scheduler:
    Enable: true
Tactical:
  Scheduler:
    Enable: true
Research:
  Scheduler:
    Enable: true
Dorm:
  Scheduler:
    Enable: true
Meowfficer:
  Scheduler:
    Enable: true
Guild:
  Scheduler:
    Enable: true
Reward:
  Scheduler:
    Enable: true

# ==================== DailyReward ====================

Freebies:
  Scheduler:
    Enable: true

# ==================== DailyMission ====================

# ==================== Opsi ====================
